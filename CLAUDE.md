# 学習ループ — 開発の引き継ぎ

中学1年生の定期テスト対策を、親が1日5分＋土曜1時間で回すためのWebアプリ。
`index.html` 1ファイルで完結（React 18 + Babel standalone をCDNから読む）。GitHub Pages（https://yuuki-lgtm.github.io/gakusyu/）に置き、iPhoneのホーム画面から使う。
この文書が唯一の仕様書。機能を変えたら、この文書の該当箇所も同じコミットで直す。

## 依頼者について
- 勇樹さん。UIデザイナー。コードは書かない。日本語のみ。返答は簡潔に、忖度なし、結論から。
- 「便利」より「続くか」を最優先する。機能追加の提案は原則しない。削る提案は歓迎される。
- 依頼は段階に分け、各段階でテストを通して git commit、push まで行う（GitHub Pages に反映されて初めて iPhone で見られる）。

## 絶対に変えない設計思想
1. **未定着リストが軸。** 点数ではなく「まだ身についていない項目」を管理する。
2. **定着の判定は白紙再生のみ。** ノートが埋まった、ワークを解いた、は証拠にならない。例外は1つ: 類題が作れなかった回だけ元の問題（ページ画像のタップ位置のまわり）を印刷してもう一度解かせ、その回は「解けた」では間隔を据え置き、「説明もできた」でのみ進める。次回は改めて類題を作る。
3. **初見と再テストを区別。** 学力の推移は初見だけで測る。再テストの点は実力ではない。
4. **項目は消えない。** 正解で間隔が伸びる（1→3→7→14→30日の5段階）。落とせば1日に戻る。30日の段階で正解した項目を「安定」と呼び、日々の再出題から外して月1の累積テストにだけ混ぜる。安定していても落とせば1日に戻る。
5. **「解けた」だけでは7日から先に進まない。** 「説明もできた」で初めて長い間隔へ（用語は除く。AIが暗記ものと判定した項目、および親が明示的に「説明不要」にした項目も除く）。
6. **3回落ちたら反復をやめて診断。** 前提の単元に戻る。
7. **親は開始の声かけ・採点・夜の確定だけ。** 横で見張らない、教えない。監査は翌日の白紙テスト。
8. **子どもが先に判定し、親が確定。** 一致率が「採点を子どもに渡す時期」の目安。
9. **紙でやらせる。** 画面を見せながら解かせない。問題はPDFで印刷。
10. **作れない形式（図・資料・長文・記述）も測る。** 学校ワークの結果を形式別に入れれば弱点として見える。リスニングは紙で反復できないので未定着に入れず、×の登録のときに形式の成績（出題数と正答数）として記録するだけ。

## 1週間の流れ（この順に画面がある。「その他→使い方」`GuideTab` と同じ内容にしておく）
- **学期の初め（1回）**: 「教材→取り込み」でワークと教科書をスキャンして取り込む。「教材→単元と進度」で教科書とワークの目次を撮り、単元とページ範囲を登録する。「その他→設定」で Supabase（同期と教材の保存先）と API キーを入れる。
- **平日・子ども**: 机の紙（前夜に印刷した類題。無ければ「紙→採点待ち」の「もう一度PDFにする」を押す）を解く → 解答ページを見て各問題の右の欄に自分で ○ か × を書く → 学校のワーク。
- **平日・親（夜5分）**: ホームに出る順に押す。昨日の紙を採点（紙を撮って読み取り、違う行だけ直して確定）→ 次の紙を作る（類題を作ってPDF）→ ワークの×を登録（ページで×をタップ）→ 今日の最後のページ（教科ごとに「p.N まで」）。各画面の最後に「次は…→」の案内があるが、押さなくてもよい。
- **土曜**: 「紙→5教科テスト」で5教科の確認テストを生成し、1つのPDFで印刷 → 解く → 赤ペンで採点 → 「紙→5教科テスト」の一覧で用紙を開いて「この用紙の答案を撮る」で結果と未定着を自動登録。
- **日曜**: 「紙→読解・記述」で読解と記述を1本ずつ。
- **定期テスト前（14日前から）**: ホームの「模試を作る（数学・英語）」の順に「紙→模試」で作って印刷。50分で解かせ、赤ペンで採点して用紙の行の「この用紙の答案を撮る」。14・7・3日前の3回。
- **返却時**: 「教材→取り込み」で答案を「テスト」として取り込み、「未定着→×を登録」で×をタップして登録する。「教材→定期テスト」で実点を入れる。
- **週1**: 「その他→依頼文」をコピーして相談。

## コードの並び（index.html）
`<script type="text/babel">` の中は「1. 定数・共通処理 → 2. データ → 3. AI → 4. 印刷 → 5. 画面」の順。各節は `/* ==== */`、その中の小分けは `/* ---- */` の見出し。新しい関数は該当する節に足す。共通処理: `unitById(d, id)`、`removeRec(d, key, ids)`（墓標つき削除）、`newItem(f)`（落とした項目の生成）、`copyToClipboard(text)`。

## 画面
画面下部に固定したメニューバー（`.tabs`、`position:fixed;bottom:0`。上のタブから 2026-09-10 に変更。黒い帯（#0B0D0C。バーの下 400px も `::after` で黒く埋め、iOS の端の跳ね返りで地の色が見えないようにする）に3つを平らに並べ、それぞれ線画のアイコン（`TAB_ICONS`、インライン SVG）の下に小さいラベル。仕切り線は入れない。選択中は白、他は半透明の白。枠や塗りのボタンにはしない（2026-09-10 に試して違和感があった）。`.main` の下余白と `.msg.fixed` の位置はバーのぶん空ける）に、アプリが扱うもの（名詞）で5つ（2026-09-11 に OOUI に寄せて変更。前は ホーム／テスト／その他 の3つ）: **今日**（`HomeTab`。やることだけ。多くて3行、残りは「ほか N 件」で畳む。要約は置かない）／**紙**（`PaperTab`: 採点待ち／次の紙／5教科テスト（ワークの結果の記録は下に畳む）／模試／読解・記述。作る・印刷する・採点するは全部「紙への操作」）／**未定着**（`ItemsTab`: 一覧（先頭に件数と教科の帯）／×を登録）／**教材**（`MatsTab`: 取り込み／単元と進度／定期テスト）／**その他**（`MoreTab` のメニュー: 分析・依頼文・設定・使い方・最新版に更新）。旧の行き先名（today／week／exam／reg）は `route()` が読み替えるので、`nextActions` や + の呼び出しは旧名のまま。内側の画面（ana／export／set／guide）は `go(tab, mode)` で直接開け、バーの選択は所属するグループ（`group`）で光る。`App` は一度開いた画面を `opened`（tab → {m, n}）に残し、`hidden` で隠すだけなので、バーで戻っても選んでいた切り替え・入力途中・スクロール位置（`scrollPos`。スクロールは body ではなく `.root` の中で行う。body は `position:fixed` で動かさず、iOS の端の跳ね返りを `.root` の中に閉じ込めて、固定のバーの下に地の色が見えないようにしている。2026-09-11）が残る。サブ画面付きの `go(tab, mode)` は `openScreen()` で n を進めて画面を作り直し、そのサブ画面から始める。バーの3つは、そのグループ（`GROUP_OF`）で最後に開いていた画面に戻す（その他→登録→ホーム→その他 なら登録）。いま居るグループをもう一度押すと先頭（ホーム／テスト／その他のメニュー）へ（2026-09-10）。定期は `go("exam")` で週末の「定期」として開く。バーの「テスト」は定期テストの14日前から「テスト あとN日」（「D-N」の表記は分かりにくいので 2026-09-10 にやめた）。ホームの「いまやること」は `nextActions(d)` が決める。
右下の +（`Fab`。5つのタブの第一階層だけ）は「紙を入れる」入口。OOUI の順で、まず撮る／選ぶ（写真も PDF も）→ 選んだものの縮小（欄の幅を測って入るだけ並べる。「+N」は出さない）、空いた枠は薄い点線で示し、右端に固定の丸い＋（縮小と同じ大きさ。文言なし。iOS のカメラは1回1枚なので、連写はこれで）、枚数は縮小の列の下の左 →「これは何ですか」の4択（昨日の紙→採点／テストの答案→用紙を選んで採点／教材→取り込む／目次→単元を登録。PDF は教材だけ）。この一覧は iOS のダークモードのシートに寄せた半透明の墨色（rgba(28,28,30,.82)＋背景ぼかし）に白い文字、上の角丸 36px、見出しは付けない（縮小と枚数の下にすぐ4択）、写真の画面が閉じきってから（0.25 秒待って）下からせり上がる（.45s）、閉じるときはシートの透明度は変えずに下へ引っ込む（.3s。幕だけ薄くなる）、上の余白 28px、一番下は「閉じる」。iOS 純正のシートは Web から使えないので真似ている。ファイルは `pending` に置き、行き先の画面が開いた時に `takePending()` で受け取る: 今日→採点は読み取りを始める（採点待ちが無ければ警告）、用紙は開いた時に読み取る、教材の取り込みは読み込み済みで開く、単元は教科と種類を選んでから「撮った写真を読み取る」。新しい処理は持たない（2026-09-11）。
各画面冒頭の説明（`Hint`）は初回だけ開いて出し、以後は右上の「?」で開閉。既読は端末の localStorage `hint_seen` に画面ごとに記憶（2026-09-11）。単元が無い項目は「—」ではなく「単元なし」と書く。
共通の見た目: 画面の中のモード切り替え（紙: 採点待ち／次の紙／5教科テスト／模試／読解・記述、未定着: 一覧／×を登録、教材: 取り込み／単元と進度／定期テスト）は下線のタブ `.subtabs`（2026-09-10 に枠つきのセグメントから変更）。種別や教科などの選択肢は枠つきのセグメント `.seg.sm` のまま。丸いチップ（教科・単元の選択 `.chip`、誤答の種類 `.et`）は `border-radius:999px` で必ず完全な半円にする（固定の px だと文字の大きさで中途半端な角丸になる。2026-09-11）。四角い印（`.tag`・`.sy`・`.cnt-b`）は 2px のまま。入力欄・選択欄・テキストエリアの文字は 14px（iOS の入力時の自動ズームは viewport の `maximum-scale=1` で止めているので 16px にしなくてよい。2026-09-11）。入力欄・選択欄・日付入力の高さは全画面で 40px に統一（2026-09-10。iOS で日付入力が2列グリッドからはみ出す不具合も同時に修正）。

### ホーム `HomeTab`
上から「いまやること」→「そのあと」→ 次の定期テストの1行（名前・あとN日・範囲内の未定着の件数。押すと週末→定期。定期テストが無ければ代わりに未定着の件数と、その下に教科の色で分けた横幅 100% の帯（`.st-bar`。全体の中でどの教科がいくつかを体感させる）と凡例。安定の数は出さない（次の行動を変えない。未定着リストの見出しにある）。押すと登録→未定着リスト）→ 使い方への動線（薄い灰の小さなボタン「使い方を見る」だけ。押すと「その他→使い方」）。ホームに使い方そのものは置かない（2026-09-11。「1週間の流れ」のカードは使い方の画面へ移した）。連続日数・未定着と安定の総数・14日のマス目のカードは 2026-09-10 に削除（親の行動を変えない数字。連続日数はゲーミフィケーション寄り）。実施ログは分析に残す。
`nextActions(d)` の先頭1つを大きく、残りを「そのあと」に。「いまやること」は黒塗りのカードのまま（2026-09-11 に白いカード＋左端の縦線を試したが中途半端で戻した）。どちらもカード全体が押せるボタン。押せることは影・右端の「›」（色は必ず明示する。iOS の button の既定色は青で、このアプリでは使わない）・押した時に沈む動きで示し、「開く」のようなボタンは足さない（2026-09-11 に一度足して情報が増えるだけだったので戻した）。順序: 単元未登録 → 進度未設定 → **昨日の紙を採点 → 次の紙を作る → ワークの×を登録 → 最後のページ** → 未採点の答案 → 模試を作る（`mockDue`: 定期テストの14・7・3日前の回ごとに範囲の全教科を、1日1〜2教科ずつ。作った用紙の `round` で進み具合を見る）** → 最初の項目 → 週末の進度更新と作問 → **累積テストを作る（前回の累積テスト `lastCumOn` から30日以上。`go("week","cum")` で作る画面を累積で開く）** → 定期テスト14日前 → 3回落ちた項目の診断 → **バックアップを書き出す（`backupOn` から30日以上。書き出すと `backupOn` を更新、同期では新しい日を採用）**。

### 夜にやること（ウィザードは 2026-09-11 に廃止）
順番を強制する4段階のウィザード（`NightFlow`・ステッパー・続きの保存）は、足場のほうが目立ち、遠回りになっていたので削除した。代わりに `nextActions` が状態のある行を独立して出す: 昨日の紙を採点（採点待ちがあるとき）→ 次の紙を作る（候補があるとき）→ 今日のワークの×を登録（教材があり今日まだ登録していない）→ 今日の最後のページ（ワーク・教科書があり今日まだ入れていない）。順番は並びで示すだけ。`LastPageStep`（教科・教材ごとに最後のページ番号を入れて「ここまで」＝`markDoneUpTo`）は「登録→落とした項目」の下に置く。採点の画面の最後に「次は次の紙を作る →」、次の紙の最後に「次はワークの×を登録する →」の小さな案内。

### 朝に紙が無いとき
「紙→採点待ち」の下の「採点待ちの分をもう一度PDFにする」で、作り直さず同じ内容を出す。ホームにあった `MorningPrint` は 2026-09-11 に削除（「今日」はやることだけにするため。常時出る予備の操作だった）。

### 紙 `PaperTab` のうち類題の紙（採点待ち／次の紙）
- **印刷 `TodayMake`**: `printSet(d)`＝明日までに期日が来る未印刷の項目（候補）。優先順は「定期テストの範囲内（`examRangeIds`）→ 再出題・再挑戦（紙で判定した記録がある項目。期日が古い順）→ 新しく登録 → 落とした回数」（2026-09-11。忘却曲線で時期が決まっている再出題を、新しく登録した項目より先に）。候補の各行に `candKind()` の印（再出題（N日あけて・前回 ○ MM-DD）／再挑戦（前回 × MM-DD）／新しく登録。期日が過ぎていれば「N日遅れ」）。`pickDaily(d)` が上限 `dailyMax()`（**問題数**。localStorage `daily_max`、既定18問、設定タブで変更。`problemCount` で3問の項目も1問の項目も合計。最低1件）までを「今日の分」に、残りを繰り越しにする。画面は候補の一覧にチェック（上位が既定で入る）で、教科ごとに見出し（`.sub-h`、教科の色の丸と件数。下線は付けない）で分け、行には単元・形式・回数だけ出す。親が外す・足すをして「この内容で作る」。外した項目は期日が過ぎたまま残り翌日の候補になる。ヘッダーと夜の作業の行に「今日 6件（繰り越し 12件）」。登録した項目は日付を持たず「次の紙」の候補になる（`nextDue` は登録日。日付の概念があるのは判定のあとの再出題だけ。2026-09-11 に「翌日」をやめた。呼び名も「明日の紙」から「次の紙」に）（一度に多数を登録したら期日を分散する `spreadDue()` は 2026-09-11 に削除。上限と繰り越しで量は守れるので二重だったうえ、項目が一覧から見えなくなって「なぜ出ない」を生んだ。起動時に `undoSpread()` が、まだ一度も紙に出ていない項目の期日を翌日に揃える）。上限から溢れた繰り越しは、週次テストの作問（`genPaper` kind 週次）がその教科の分を1問ずつ混ぜ（問題数は +件数、上限30）、問題に `itemId` を付ける。答案を撮ると `judgeCarried()` が○を「解けた」、×・空欄を「できなかった」として元の項目を判定し、新しい項目は作らない。`genForItem()` で類題を自動生成（今日作った類題があれば再利用。行の「作り直す」を押した項目は新しく作る）し、`genToPaper()` で1つの用紙にして `exportPDF()`。成功した項目に `printedOn` と紙の中の順番 `printedAs` を付ける。生成に失敗した項目のうち、元の問題（`src` の座標）があるものは `printedOrig:true` にして、`genToPaper()` が元の問題の切り出し（`refs` の `crop`、`resolveRefs()` が `cropAround()` で切る）を資料に載せ「元の問題（図N）をもう一度解き、なぜそうなるかを説明する」を1問置く。元の問題が無い項目は次回に回る。「採点待ちの分をもう一度PDFにする」は再生成なしで同じ内容を出す。
- **採点 `TodayGrade`**: `gradeSet(d)`＝`printedOn` のある項目を印刷日→紙の順（`printedAs`、印刷時に付く）。`printedOrig` の回（元の問題）は `applyJudgment` が「解けた」で level を据え置き（次回は同じ間隔）、「説明もできた」でだけ進める。履歴に `orig:true`。判定か「やっていない」で印は消え、次回は改めて類題を作る。「紙を撮る」で `readSelfMarks()` が紙の各問題の右端の四角い欄（`jb`）の ○× を AI で読み（項目ごとに、説明の問いを除く問題が全部 ○ なら「解けた」、1つでも × なら「できなかった」、説明の問いも ○ なら「説明もできた」。`selfMarksText()` が項目ごとの問題番号を渡す）、`applySelfMarks()` で子ども＝親の初期値として入れる（× は前回の誤答の種類、用語の「説明もできた」は「解けた」に）。親は違う行だけ直す。「子ども」の行はボタンを持たず、紙から読み取った値の表示だけ（2026-09-11。以前は手で押す行があり二重に見えた）。押せるのは「親」の行だけ。撮らないときは親の行を押す。紙に「問題が変」の欄は無く、行の「問題が変」を押すと、`judgeAll` がその項目を判定せずに印刷と類題を消して翌日に作り直す（`badN`、`d.badCount`）。分析に「問題が変 N件 / 生成 M件」。「できなかった」は誤答の種類が必須（前回の種類が初期値）。「まとめて確定」で `judgeAll()`。「やっていない」は `printedOn` を外して次の紙に戻す。「全部を次の紙に戻す（判定しない）」で採点待ち全部を一度に戻せる。用語は「解けた」で進み、それ以外は「説明もできた」でないと7日止まり（`applyJudgment` のまま）。
- 採点の行をタップすると展開（`GradeRowDetail`）: 2回以上落ちた項目は「考え方」の説明、類題と解答、3回以上落ちた項目は「診断」（`diagnose()`、結果は `item.diag`）。診断の各手順に「この手順を項目として登録」があり、`stepToItem()` が AI を呼ばずに手順の文を項目名にして未定着に入れる（`from` に元の項目、手順には `itemId`）。翌日の紙に類題が出る。画面で1件ずつ判定するカードは 2026-09-10 に廃止（紙の運用ができたため。画面を見せない原則）。`pending` は旧データにだけ残る。

### 紙 `PaperTab` のうち週末の紙（5教科テスト／模試／読解・記述。「ワークの結果を記録」は5教科テストの下）
- **作る `MakePapers`**: 教科ごとに `pickUnits()`（未定着がある単元と最近出していない単元）か手で選んだ単元で `genPaper()`（週次・累積・模試で共通の作問関数。AI 節）を呼び、`materialsForUnits()` の教材ページを「図1〜図N」として添付して作問。問題文が参照した図（「図3」または `fig`）のページだけ `refs` に残す。「今日作った N 教科を1つのPDFに」で `exportPDF(papers)`。各行の `PaperRow`→`PrintSheet` は1教科ずつ。
- **模試 `MockMaker`**: 「定期テスト」で範囲を選んだ直近のテストの範囲で（未登録なら「定期テストを登録する」ボタンで定期テストのタブへ）、1教科ずつ `genPaper(kind:"模試")` で作る。用紙は `examId`・`examName`・`left`・`round`・`minutes:50`・`maxScore:100` を持つ。形式は本番に寄せる: 大問は教科ごとの `MOCK_SECTIONS`（知識→計算→応用→記述の順）、各問に配点 `pts`（`normalizePts` で合計をちょうど100に）、`sec` に大問名。用紙には大問の見出し・配点・50分・100点満点が出る。教材種別「テスト」の答案（`testSheets`、新しい4枚まで）があれば `refSheets` で画像にして【参考】として渡し、大問数・配点の配分・記述の量を寄せる（問題文は写させない）。週次の判断（`paperThisWeek`）や「今日作った教科をまとめてPDF」には混ぜない。模試の用紙は再利用しない。
- **答案を撮る `PaperGrade`**（独立したモードではなく、各用紙の行 `PaperRow` を開いた中。未採点の用紙だけ）: 採点済みの答案を撮り、AI が○×を読む。`gradeResult()` で形式別の結果に集計し、×と空欄を未定着に入れる。模試なら配点で `score`／`maxScore`（100点換算は `pct`）と `examId` も `tests` に入れる。
- **読解 `ReadingMaker`**: 新規の文章と設問を作る（既存作品は使わない）。落とした設問は「読解の技能」として未定着に。
- **記述 `WritingFlow`**: 課題を作り、答案を撮って添削。表面の誤りの種類を未定着に。
- **手入力 `ManualTest`**: 学校のワーク等の結果を形式別に入れる。

### 未定着 `ItemsTab`（一覧／×を登録）と 教材 `MatsTab`（取り込み／単元と進度／定期テスト）
- **単元 `UnitReg`**: 「教科書の目次／ワークの目次」を撮って `applyTOC()`（既存の単元名に対応づけてページだけ足す、無ければ新規）。単元は `pages`（教科書）と `wbPages`（ワーク）を別々に持ち、名前の下をタップして直せる。進度は「ここまで」で手動、または `applyAutoProgress()` で自動（ワークの×を登録したページを含む単元）。
- **落とした項目 `ItemReg` → `TapReg`**: 教科→教材（ワーク／教科書）→ページを開き、×だった問題を指でタップ。青い印が付き、その場で誤答の種類の4択（初期値「知らなかった」）を選べる。印をもう一度タップすると4択と「この印を消す」。選択肢はページ画像の上、印のそばにポップオーバー（`MarkPop`: 描いてから実際の大きさを測り、見える範囲＝拡大中は枠・そうでなければ画面（下のバーぶんを除く）に収まる位置へ動かす。右にはみ出せば印の左、下にはみ出せば印の上、それでも出れば範囲の中に寄せる。画像の下に別枠で出さない）。ページをまたいで持てる。「まとめて登録」で `tapsToItems()`。保存するのは座標だけ。1倍／2倍／3倍の拡大。画面の印は 10px の小さな青い丸（指の当たり判定は 22px のまま。印が大きいと隣の問題にかぶって見える）。登録済みの×は薄い印で出て、タップすると状態と誤答の種類の変更。最後に「今日やった最後のページを開いて『p.N までやった』」を1タップすると、そのページまでの取り込み済みページが `doneOn` になる（`markDoneUpTo`）。×がない日もこれだけ押す。教材の種別が1つしか無い教科では、種別の切り替えは押せない1つの表示（`.seg.one`）にする。教材が無い教科は手入力の1件追加だけ（「手で1つ追加」の折りたたみは教材の有無にかかわらず閉じた状態で出す）。
- **未定着一覧 `ItemList`**: 教科ごとに未定着と安定の件数、各項目は項目名の下に「どういう問題だったか」（`note`。タップ登録なら AI の要約、手入力なら間違え方）、その下に「いつの紙に出るか」（`paperStatus()`: 次の紙に出る／MM-DD の紙に出る／採点待ち／定期テスト後／安定。2026-09-11。「未定着にあるのに次の紙に無い」が分からなかったため。次の紙の先頭にも候補に入っていない件数と理由を出す）、項目の削除（× の直後に「取り消す」を6秒出す。取り消すと墓標から外して戻す）、誤答の種類の変更（小さいチップをタップすると4択のポップアップ）、教科を直す（取り込みで教科を間違えた分の救済。単元は外れる）、「説明あり／説明不要」の切り替え1つ（用語以外。バッジは重ねて出さない）。仮の名前には「仮」の印（改行しない）。仮のままのタップ登録の項目は、アプリを開いたとき（`App` の効果。同じ項目は1回の起動で1回だけ）に裏で `nameAndApply()` を呼んで付け直し、付いたら通知を出す。それでも仮なら一覧の上に1つだけ「仮の項目 N 件に名前を付ける（AI）」があり、教科ごとにまとめて `nameAndApply()`（登録直後の名前付けと同じ処理: 同じ技能なら統合、リスニングは外す）を呼ぶ（項目ごとのボタンは押す手間が多いのでやめた）。×登録の印のポップアップでも切り替えられる。
- **教材 `MaterialReg`**: 下の「教材画像」を参照。

### 分析 `AnaTab`／定期 `ExamTab`／依頼文 `ExportTab`／設定 `Settings`
- 分析: 保持率（30日以上あけた再出題の正答率）、形式別、子どもと親の判定の一致率、説明不要の項目の件数（用語＋印あり）、誤答の種類の分布。見るだけ。
- 定期: テストの登録と範囲。範囲内の未定着、未出題の単元、**ワークで手つかずのページ（`untouchedPages`）**、実点と予測のずれ（`predictFor`: その定期テストの模試の100点換算を優先、無ければ直近の累積テスト）。**14日前から `deferForExam()` が範囲外の項目の再出題日をテスト翌日に後ろ倒しする**（起動時と保存のたびに適用。`level` は変えないので、判定すれば元の間隔で進む。印刷済み・判定中・単元なしは触らない）。後ろ倒し中の件数を表示（「紙→次の紙」の先頭にも、教科ごとの件数と理由を橙の枠で出す。2026-09-11。数学・理科が消えたように見えた問い合わせがあった）。
- 依頼文: 学習状況を文章にしてコピー。
- 設定: 設定を別の端末に渡すリンク（`cfgLink`／`importCfg`、#cfg= で開くか貼り付け）、Supabase（URL・anon キー・共有ID。SQL は未設定のときだけ開き、設定済みなら畳む）、API キー、バックアップ、バージョン。

## データ構造（v3）
```
{ v:3, updatedAt,
  units:[{id, subject, name, pages(教科書), wbPages(ワーク), learnedOn(習った日|null), learnedBy('auto'|undefined), lastTestedOn, updatedAt}],
  items:[{id, subject, unitId, label, note, fmt, etype, createdOn,
          history:[{d, r:'x'|'o'|'oo', self, etype, etypeSelf}],
          level(0-5), failCount, nextDue, status:'active'|'stable',
          pending:{d, self, selfE, pm}|null, gen, genOn(類題を作った日), printedOn(紙に印刷した日|null), diag,
          src:{path, kind, page, x, y}|undefined(教材のページとタップ位置。旧データは q=問題番号), named(false なら仮の名前),
          defer:{examId, from}|null(定期テスト前の後ろ倒し中), noWhy(true なら説明不要＝暗記もの), printedOrig(true なら元の問題を印刷した回),
          guide:{explain, example:{q,a}|null, pages, fc, on}|undefined(2回以上落ちた項目の考え方の説明), updatedAt}],
  tests:[{id, subject, date, kind:'週次'|'累積'|'定期'|'読解'|'模試', source, rows:[{fmt,total,correct}], total, correct, score, maxScore, examId(模試のみ), unitIds, paperId, updatedAt}],
  papers:[{id, code, subject, date, kind('週次'|'累積'|'読解'|'模試'), title, passage, unitIds, questions:[{n,q,a,unitId,fmt,aim,label,svg,fig, sec, pts}],
          examId, examName, left, round, minutes, maxScore(模試のみ),
          refs:[{n, kind, page, path}](資料にする教材ページの参照), imgs:[b64](旧データの写真), status:'printed'|'graded', model, updatedAt}],
  log:{'YYYY-MM-DD':true}, exams:[{id,name,date,unitIds,actual:{教科:点},updatedAt}],
  writing:[{id,date,subject,len,structure,surface,note,updatedAt}],
  materials:[{id, subject, kind:'ワーク'|'教科書'|'テスト', page, path, doneOn(やった日|null), idx:{ns:[問題番号], at}|undefined, updatedAt}],
  backupOn(最後に書き出した日|null), genCount(類題の生成回数), badCount(「問題が変」の回数), deleted:[id] }
```
- 定数: `FORMATS`（出題形式8種）、`ETYPES`（誤答の種類4種）、`INT=[1,3,7,14,30]`（level 0〜4）、`STABLE_LEVEL=4`（30日の段階。ここで正解したら安定。`migrate` が旧の level 5 を 4 に丸める）、`MKINDS=["ワーク","教科書","テスト"]`（`BOOK_KINDS` は目次のある教科書とワークだけ）、`SUBJ_CODE`、`KIND_CODE`（wb/tb/ts）。
- `applyJudgment(item, r)` が間隔反復の核。変えるときは必ずテストを通す。
- 日付は `today()`／`addDays()`／`diffDays()` で、端末のローカル日付の文字列で扱う（`toISOString` は UTC になり日本で1日ずれるので使わない）。
- `migrate()` で旧データ（v2）を引き継ぐ。`{...blank(), ...d}` で足りない配列を補う。壊さない。
- 保存は `App.save(next)`。`next` は新しい状態か、`(cur) => 状態` の関数（AI の名前付けなど遅れて戻る更新は必ず関数で。古い `d` で上書きしない）。localStorage の `gakushu_loop_v3` に全体を入れる。画像の base64 は入れない（5MB制限）。
- 同期は Supabase の `state` テーブル1行に全体をJSONで保存（共有ID = 行の id）。`mergeData()` で配列ごとに id 単位で `updatedAt` の新しい方を採用。`deleted` は墓標。`materials` も同じ。

## 教材画像（ワーク・教科書）
- Supabase Storage の非公開バケット `materials` に置き、端末には索引 `materials[]` だけ持つ。家庭内の私的使用に限る。パスは `共有ID/math/wb/12.jpg`（教科×教材種別×ページ）。種別「テスト」は返却された定期テストで、枚をページとして扱う。紙の役割 `role` を持つ: `qa`（問題と答案が同じ紙、既定。旧データも同じ扱い）、`q`（問題用紙、パスは `ts/q1.jpg`）、`a`（答案、`ts/a1.jpg`）。問題用紙と答案は同じ番号同士が対（`pairSheet`）。×の登録は答案と同じ紙だけをめくり（`answerSheets`）、答案に対の問題用紙があれば `src.qpath` に持って、名前付け・類題生成で答案と問題用紙の2枚を AI に渡す（「答案の印の位置の答えに対応する問題を問題用紙から探す」）。元の問題の印刷は問題用紙のページ全体を図にする。対が無い答案は画面に「問題用紙 n枚目が未取り込み」と出る。単元のページ範囲とは結びつけない（作問の添付・進度の自動判定に混ざらない）。単元は画面の「単元」で指定する。共有IDがアクセスの鍵。`stUpload`／`stGet`（メモリに40件までキャッシュ）／`stRemove`。
- 設定タブの SQL でバケットとポリシーを作る（2026-09-10 に実行済み）。
- 取り込み `MaterialReg`: まず PDF（pdf.js で1ページずつ画像化）か複数画像を選び、そのあとに教科（既定なし。選ぶまで保存できない。2026-09-11 に数学のまま理科を入れる事故があった）・種別・（テストなら紙の役割）・綴じ方向・最初の番号を決めて保存する（2026-09-11 に順番を入れ替えた。先に条件を決めさせない）。名前順に番号を振る。最初の番号の既定は `nextPageNo()`（その教科・種別・テストなら紙の役割ごとに、取り込み済みの最後の番号＋1。無ければ 1）で、教科・種別・役割を切り替えるたびに合わせ直す（問題用紙→答案で 1 に戻し忘れるのを防ぐ）。画像が横長（幅 > 高さ×1.15）なら見開きとして `splitSpread()` で2ページに、縦長なら1ページ（`assignPages`）。切る位置は `findGutter()` が推定（中央付近の暗い縦帯＝綴じ目の影、無ければ空白の縦帯、無ければ真ん中）。国語など右綴じは右ページが若い番号（教科で自動。切り替えの文言は「横書き（左が若い）／縦書き（右が若い）」）。同じページの入れ直しは上書き（`doneOn` は引き継ぎ、索引は捨てる）。削除はページ範囲指定か全部、確認つき。
- 単元との紐づけ: `parsePages("p.12-15, 20")` → 数値の配列。`unitPages(u, kind)`、`unitForPage(units, kind, page)`。
- 作問への添付: `materialsForUnits(d, subject, units)` が単元のページ範囲に入る教材を集め、多いときは教材ごとに均等に間引く（上限16）。読めなければ添付なしで続ける。
- 用紙への印刷: 問題が参照したページだけ `refs` に持ち、`resolveRefs()` が PDF 生成時・HTML 保存時に Storage から読んで資料ページに描く。プレビューは枠だけ。

## ×の登録（タップ）と AI
- 保存するのはタップ位置（`src:{path, kind, page, x, y}`、0〜1の割合）だけ。仮の名前「ワーク p.12 の×（1）」・`named:false`・誤答の種類「知らなかった」・翌日に出る。
- 登録直後に `nameItems()` がページごとに1回 AI を呼び、`annotateB64()` でページ画像に番号つきの青い印（小さな点と右横の番号。幅の 0.7%。色は青。赤だと答案に元からある赤ペンの ○×✓ と混同され、印の無い問題まで拾われた。2026-09-10 まで幅の 2.8% の丸と上の番号だったが隣の問題にかぶって別の問題を読むことがあった）を描いて渡して、印ごとの項目名と形式、暗記ものか（`rote` → `noWhy`）を付ける。項目名は「〜力」で終わらせず動詞で止める（「濃度の式を立てる」。2026-09-11）。AI には同じ教科の既存の未定着項目の一覧も渡し、同じ技能（後日その一点を聞き直せば同時に確かめられる）なら `same` に番号を返させる。`mergeSameSkill()` が新しい項目を作らず（墓標）、既存の項目の落とした回数を増やして level 0・期日を翌日にする（履歴に `merged:true`）。迷えば別項目。形式が「リスニング」の項目は `splitListening()` が未定着から外し（墓標に入れる）、紙ごとに `tests` へ「定期／リスニング／出題数（AI が数えた `listening_total`、×より少なければ×の数）・正答数＝出題数−×」を記録する。分析の形式別に出る。反復はしない（設計思想10）。`needsWhy(i)` が false（用語または `noWhy`）の項目は、判定・類題の紙・採点画面・読み取りで用語と同じ扱い（説明を求めない）。失敗しても仮の名前のまま動き、類題生成のとき `applyGen()` でもう一度付ける。応答は `parseItemsLoose()` で読む（JSON が途中で切れていたり前後に文が付いていても、完成している項目だけ拾う。出力の上限は 3000 トークン）。失敗の理由（画像を読み込めない／AI の呼び出し失敗（空の応答なら止まった理由つき）／応答を読めない（先頭 60 字つき）／AI が印 N の問題を特定できない）は `meta.errors` に入り、登録直後と「名前を付ける」の結果の文に出る。
- 類題 `genForItem()`: 元の問題があれば印つきの画像を渡す（`genContent`）。問題数は `problemCount(i)`: 習得中（間隔1日・3日）は3問、保持の確認（7日以上）は1問、暗記もの（`needsWhy` が false）は常に1問。落として1日に戻れば3問に戻る。用語は「〜を書きなさい」の記述形式、説明を求める項目には「説明させる問い」を1問。判定は「出した問題がすべて正解」で「解けた」。今日の分は問題数が合わなければ作り直す。2回以上落ちた項目、または誤答の種類が「知らなかった」の項目（初回から。`needsGuide`）には `guideFor()`（Opus 5）で「考え方の説明」（3〜5行、平易。安定した前提の項目を踏まえる）と例題1つ、教科書のページ（単元の `pages`）を作って `item.guide` に持ち、落とした回数が変わるまで再利用。載せるのは初回と再挑戦（前回 ×）の回だけで、正解して間隔をあけて戻ってきた再出題の回には載せない（白紙再生を確かめる回。2026-09-11）。用紙では類題の前に番号なしの枠（`guide:true` の行）で出し、解答ページと満点には数えない。説明が作れなくても類題は出す。数学・理科の計算、英語の文法・英作文（`needsCheck`）は生成後に `verifyGen()` が別の呼び出しで解かせて解答と照合し、不一致なら作り直す（最大2回）。2回とも不一致なら「類題が作れなかった項目」と同じ扱い: 元の問題があれば `printedOrig:true` にしてその日のPDFに元の問題を切り出して載せ、説明を求め、「解けた」では間隔を据え置く。元の問題が無ければ翌日に回す（`gen` を消す）。用語・国語・社会は検算しない。検算を通った類題は `gen.checked:true`。生成回数は `d.genCount` に足す。読み間違いはカードの「別の問題」で作り直す。
- 索引は裏で持つ。`doneOn`（×を登録した／「p.N までやった」を押した日）と `idx`（ページを開いたときに `buildIndex()` が問題番号を書き出す。1ページ1回、失敗はセッション内で再試行しない）。用途は `applyAutoProgress()`（進度の自動判定）と `untouchedPages()`（定期テストの手つかずページ）だけ。索引が外れていても登録と類題には影響しない。
- 廃止したもの: 「答案の写真から候補を出す」、「問題番号の入力」。手入力の1件追加は残す。

## 印刷（PDF）
- `paperHTML(p, res)` で用紙HTML。シートは `data-part="q"`（資料・問題）と `"a"`（解答）。`genToPaper(items)` は類題を用紙にする（項目の区切りに小見出し `sec`: 教科が複数あれば「教科・単元」、1教科なら単元だけ、単元が無く1教科なら無し。問題文の中には入れない。説明の問いは 7 日の段階（`level` 2）以上の項目にだけ付ける。各問題の右端に ○× を書く 15mm 角の欄 `jb`（子どもの ○ は 8〜12mm なので 11mm では小さい）。読み取りは ○ 以外の印（×・✓・／・△・塗りつぶし）をすべて不正解として扱う。元の問題の回は「解く」と「説明」の2問。紙の上部に `SELF_NOTE` の注意書き）。チェック欄の4択「できなかった／解けた／説明もできた／問題が変」は 2026-09-10 に廃止（子どもに毎日4択を読ませるのは重い。決まった位置の ○× のほうが読み取りも確実）。
- `buildPDF(papers)` は用紙の配列を受け、html2canvas + jsPDF でA4に詰める（ブロックごとに画像化、境目で切らない）。各用紙の「資料＋問題」を先に並べ、用紙ごとに奇数ページなら白紙を足して偶数にし、解答は全用紙ぶんを最後に教科で改ページせず詰める。両面印刷1回で解答が必ず別の紙になる。今日の類題PDFも5教科の確認テストも同じ経路。
- `exportPDF()`: iOS は `navigator.share` で共有シート、それ以外はダウンロード。「HTMLで保存」は予備で、白紙の調整は入らない。
- 既知の問題: html2canvas は oklab/oklch を読めないので独立 iframe 内で描画。html2canvas は詰め（`font-feature-settings: palt`）やカーニングがあると「【」「」」や数字がずれて消えるので、iframe 内では詰め・カーニング・字間をすべて切る（画面のプレビューだけ palt）。文字は A4 に対して本文 12.5px（約9.5pt）、解答 12px、罫線の間隔 21px（2026-09-10 に iPhone で確認して決めた）。iOS Safari は画面より広い iframe の文字を自動で拡大する（text-size-adjust）ので、iframe に viewport meta と `-webkit-text-size-adjust:none` を入れている（これが無いと iPhone だけ文字が1.5倍ほどになる。Chrome では再現しない）。実ブラウザで確認すること。2026-09-10 に Windows Chrome で 2教科6ページと類題3ページ（問題＋白紙＋解答）を確認済み。

## AI
- Anthropic API を直接呼ぶ（`anthropic-dangerous-direct-browser-access`）。キーは localStorage。モデルの使い分け: 類題生成・答案や紙の読み取り・目次と索引・項目名付け → `claude-sonnet-5`（`SONNET`）、診断・同じ技能かの判定（既存の一覧を渡すとき）・模試の作問 → `claude-opus-5`（`OPUS`）。`callAI(content, system, maxTokens, model)` は失敗したら Sonnet 5 で1回やり直す（Sonnet が失敗したら Sonnet でもう一度）。API のエラーは `apiErrorText()` が日本語にする（残高切れ・回数制限・キー無効・混雑。元の文を添える）。各画面はその文を赤のスナックバー（またはやり直しボタン付きの枠）に出す。ただし利用者側で直すまで使えない状態（キー未設定／キー無効／残高切れ。`apiBlock`、localStorage `api_block`）は、スナックバーではなくヘッダーの下の帯 `Banner` に全画面で出し続け、キーを保存するか呼び出しが成功したら消える（2026-09-11）。設定の APIキーの節に、帯の見本を出す／消すリンクがある（見た目の確認用）。回数制限・混雑は一時的なのでスナックバー。設定の「すべて Opus 5 を使う」（localStorage `all_opus`）で全部 Opus に（`pickModel`）。
- 出力は必ず JSON 指定、`parseJSON()` で取り出す。画像は `imgBlock(b64)`。
- 用途: 類題生成／診断／採点済み答案の○×読み取り／目次から単元抽出／タップした問題の項目名付け／ページの問題番号の索引／作問／読解の文章と設問／記述の添削／設定の接続確認。
- 教科書・既存作品の文章は複製しない。文章は新規に書く。教材ページは「用語・表現・難易度を合わせる」ためと図の参照のために渡し、本文や設問をそのまま写させない。

## 同期・設定
- Supabase の URL・anon キー・共有ID・API キーは端末ごとの localStorage。iOS はホーム画面のアプリと Safari で保存場所が別。
- 「設定を別の端末に渡す」: `cfgLink()` が入っているもの全部を `#cfg=base64(JSON)` のリンクにする。開くと起動時に `importCfg()` が取り込む。ホーム画面のアプリには貼り付け欄で。
- 起動: localStorage → `migrate` → 同期先があれば `pullRemote` と `mergeData` → 表示。保存は1.5秒まとめて `pushRemote`。

## テスト
`npm test` で全部走る（`npm install` を一度だけ。node の組み込みテストランナー）。**修正したら必ず通す。** 2026-09-10 時点で321件。
- `test/load.js` — index.html の `<script type="text/babel">` を取り出し、Babel で JSX を変換して node で評価する。localStorage / document / window / navigator は最小のスタブ。**関数やコンポーネントを足したら `EXPORTS` に名前を追加する。**
- `test/fixtures.js` — デモ／空／pending あり の3状態。日付は今日基準の相対。スキーマを変えたらここも直す。
- `test/render.test.js` — `renderToString` で全タブ・全サブモードと用紙プレビューを3状態で描画。React の警告、画面に出る `undefined`／`NaN` も失敗にする。
- `test/core.test.js` — 純関数の単体テスト（間隔反復、日付、統合、用紙、Storage、教材、タップ登録、索引、PDFの組み方、設定リンク）。`fetch` はスタブで差し替える。
- 実ブラウザ確認: Chrome 拡張は file:// を開けないので `python -m http.server 8765 --bind 127.0.0.1` で `http://127.0.0.1:8765/index.html`。デモデータは `test/fixtures.js` の `demo()` を localStorage に入れる。教材画像は `_fake/storage/v1/object/authenticated/materials/...` を作って `sb_url` を `http://127.0.0.1:8765/_fake` にすると読める。終わったら消す。

## バージョンと運用
- ヘッダーは1行だけ。左から「学習ループ・同期タグ（同期先があるときだけ。`.sy`）・（右端）バージョン `.ver`」。件数（未定着・今日・安定・連続）の行は 2026-09-10 に外した（ホームの数字カードと重複し、他の画面では判断に使わない。連続日数を常時見せない）。
- `index.html` の `VERSION`（ヘッダー右端と「その他」の更新ボタンに表示）は、git の pre-commit フック `tools/pre-commit` がコミット日時で自動更新する。別の環境では一度 `sh tools/install-hooks.sh`。
- push すると GitHub Pages に1〜2分で反映。iPhone で古いままなら、「その他」の「最新版に更新（再読み込み）」（クエリを付けて `location.replace`）。それでも古ければアプリを完全に終了して開き直す。
- ブラウザの `alert`／`confirm` は使わない（削除の確認は画面内の2択）。
- 操作の結果（保存した・登録した・読み取った）とエラーは、画面上部（ヘッダーの上に重なる。上から降りてきて上へ消える。2026-09-11 に下から移した。右下の + と重ならないため）のスナックバー `notify(text, kind)`（`Snacks`。2026-09-10）で出す。ok（緑の帯・先頭に ✓）と warn（橙・!。入力の不備）は 4.5 秒で消え、err（赤・ⓧ）は × を押すまで残る。やり直しのボタンが要るエラー（診断・紙の読み取り・答案の読み取り）だけは、その場の `errbox` にボタンと一緒に出す。処理中の表示（busy）はその場のまま。

## 決めたことと理由
- 下のバーは「やること」ではなく「扱うもの（紙・未定着・教材）」で分ける（2026-09-11、OOUI）。今日の画面はやることだけにし、要約（未定着の件数と帯）は未定着の一覧の先頭へ。作った本人が迷ったのは、タスクの一覧と状態の要約が同じ画面に混ざっていたため。戻す可能性はある。
- 画面のラベルは名詞で「何を作る・何を見るか」が分かる形にする（2026-09-11。「作る」「次の紙」のような動詞だけ、「定期」のような省略は避ける。下のバーの「週末」は「テスト」に）。ボタンも「次の紙を作る」「N件を未定着に入れる」のように対象を書く。「落とした項目」はそのまま。
- 教材画像を Storage に置く: 端末の localStorage 5MB を超えないため。用紙も画像を持たず参照だけ。
- 作問に添付した教材ページのうち、問題が参照したものだけ印刷する: 毎回16ページ印刷すると続かない。
- 「今日」を紙で回す: 親が口で聞く運用は続かない。子どもが解答を見て自分で判定するのは設計思想8のまま。
- ×の登録をタップ式にし、索引や番号の一致に依存しない: 索引が外れても登録と類題が止まらない。
- 見開きを自動判定して綴じ目で切る: 片ページだけの1枚を別に入れる手間をなくす。
- 模試は本番の予行で、1回きり: 用紙は再利用せず（回ごとに新しく作る）、週次の判断・累積の間隔・まとめてPDF・今日の印刷には混ぜない。×は通常どおり未定着に入り、点は「定期」の予測にだけ使う（設計思想3: 学力は初見で測る。模試は初見）。
- 日付はローカル: `toISOString` で1日ずれていた既存バグを 2026-09-10 に修正。
- 定期テスト前の後ろ倒しは `nextDue` だけ動かし `level` は保つ: 設計思想4（間隔は正解で伸び、落とせば戻る）を崩さずに、14日前から範囲だけに集中させるため。

## やらないこと
- 配色を白ベースにすること。2026-09-10 に純白＋中立の灰を試して戻した（緑がかった灰 #F6F7F5／#DDE2DD／#1E2321 の元の色味を維持する）。
- 通知、ゲーミフィケーション、子ども向けダッシュボード、画面上での出題。
- ピンチ拡大（アプリ全体が誤って拡大されるため。ページ画像は倍率の切り替えで）。
- 機能を足す前に「続くか」を問う。迷ったら足さない。

## 次の作業
1. iPhone で実機確認（2026-09-10 の実装はテストと Windows Chrome のみ）。**確認項目の一覧は `docs/iphone-check.md`**。済んだものはそこに印を付け、見つかった不具合はここの「次の作業」に足す。
   - 「教材→取り込み」で見開きの PDF を取り込み、綴じ目の位置とページ番号が合うか。pdf.js が iPhone Safari で動くか。
   - 「未定着→×を登録」でタップ登録し、数秒後に項目名が付くか。印の当たり判定（22px）が指で押しやすいか。拡大の使い勝手。
   - ホームの「次の紙を作る」で類題の生成と PDF（共有シート）、翌日の「昨日の紙を採点する」でまとめて確定。
   - 「紙→5教科テスト」で教材の添付、図を参照した問題の資料ページ、「1つのPDFに」。
   - 「教材→定期テスト」に手つかずのページが出るか。
   - 定期テストの14日前に「模試を作る」が出て、模試の生成（大問・配点・返却テストの参考）、印刷、用紙の行の「この用紙の答案を撮る」で100点換算、「教材→定期テスト」の予測に模試が使われるか。
2. Supabase 同期を2端末で確認。
3. コードの整理は 2026-09-10 に実施済み（並べ替え、共通化、`.cnt`／`.num` の分離）。
